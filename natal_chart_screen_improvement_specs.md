# Техническое задание на доработку экрана "Натальная карта" - AstroWise

## 📋 Резюме задачи

**Цель**: Кардинально улучшить UX/UI экрана натальной карты, сделав его понятным и доступным для пользователей любого уровня астрологических знаний.

**Ключевые проблемы текущей версии**:
- Информационная перегрузка без структуры
- Отсутствие пояснений и подсказок
- Сложная навигация по элементам карты
- Нет адаптации под уровень знаний пользователя
- Отсутствие обучающих материалов

---

## 👤 User Stories

### Новичок в астрологии
1. **Как новичок**, я хочу понимать, что означают символы и термины на карте, **чтобы** изучать астрологию постепенно
2. **Как новичок**, я хочу видеть простое объяснение своей карты, **чтобы** получить базовое понимание без изучения сложных терминов
3. **Как новичок**, я хочу иметь интерактивный тур по карте, **чтобы** понять, на что обращать внимание в первую очередь

### Продвинутый пользователь
4. **Как опытный пользователь**, я хочу быстро переключаться между режимами отображения, **чтобы** видеть нужную информацию без лишних пояснений
5. **Как опытный пользователь**, я хочу видеть все аспекты и орбисы, **чтобы** проводить детальный анализ
6. **Как опытный пользователь**, я хочу настраивать отображение элементов карты, **чтобы** фокусироваться на важном

### Любой пользователь
7. **Как пользователь**, я хочу сохранять интересные интерпретации, **чтобы** возвращаться к ним позже
8. **Как пользователь**, я хочу делиться отдельными аспектами карты, **чтобы** обсуждать их с друзьями
9. **Как пользователь**, я хочу видеть, как моя карта меняется с транзитами, **чтобы** понимать текущие влияния

---

## 🎯 Функциональные требования

### 1. Режимы отображения

#### 1.1 Режим "Новичок" (по умолчанию для новых пользователей)
```swift
enum DisplayMode: String, CaseIterable {
    case beginner = "Простой"
    case intermediate = "Расширенный"
    case expert = "Эксперт"
}
```

**Особенности режима "Новичок"**:
- Показывать только основные планеты (Солнце, Луна, Меркурий, Венера, Марс)
- Упрощенные интерпретации без терминологии
- Визуальные подсказки и иконки вместо символов
- Скрыть сложные аспекты (показать только соединения, оппозиции, трины)

#### 1.2 Режим "Расширенный"
- Все планеты включая социальные (Юпитер, Сатурн)
- Основные аспекты с краткими пояснениями
- Дома с базовыми значениями
- Возможность углубиться в детали по клику

#### 1.3 Режим "Эксперт"
- Полная информация как сейчас
- Все аспекты с орбисами
- Транснептуновые планеты
- Детальная информация о домах и куспидах

### 2. Интерактивный онбординг

#### 2.1 Первое открытие экрана
```swift
struct ChartOnboardingFlow {
    let steps: [OnboardingStep] = [
        .welcome,           // "Добро пожаловать в вашу натальную карту!"
        .sunExplanation,    // "Это ваше Солнце - ваша сущность..."
        .moonExplanation,   // "Луна показывает ваши эмоции..."
        .ascendantInfo,     // "Асцендент - как вас видят другие..."
        .navigation         // "Нажимайте на элементы для деталей"
    ]
}
```

#### 2.2 Подсветка элементов
- Пульсирующая подсветка активного элемента в туре
- Затемнение остальных элементов
- Стрелки-указатели с пояснениями
- Кнопка "Пропустить тур" всегда видна

### 3. Информационная архитектура

#### 3.1 Новая структура экрана

```
┌─────────────────────────────────────┐
│  Переключатель режимов    [?] Помощь│
├─────────────────────────────────────┤
│                                     │
│        ВИЗУАЛИЗАЦИЯ КАРТЫ           │
│         (адаптивная)                │
│                                     │
├─────────────────────────────────────┤
│  Вкладки: Обзор | Планеты | Дома | │
│           Аспекты | Транзиты        │
├─────────────────────────────────────┤
│                                     │
│     КОНТЕНТНАЯ ОБЛАСТЬ              │
│     (меняется по вкладкам)          │
│                                     │
└─────────────────────────────────────┘
```

#### 3.2 Вкладка "Обзор" (по умолчанию)
```swift
struct OverviewTab: View {
    // Карточки с ключевой информацией
    var sections: [OverviewSection] = [
        .personality,      // "Ваша личность" - Солнце, Луна, Асцендент
        .strengths,        // "Сильные стороны" - благоприятные аспекты
        .challenges,       // "Зоны роста" - напряженные аспекты
        .lifeThemes       // "Жизненные темы" - выделенные дома
    ]
}
```

### 4. Система подсказок и пояснений

#### 4.1 Контекстные подсказки
```swift
struct TooltipSystem {
    // Показывать при наведении/долгом нажатии
    func showTooltip(for element: ChartElement) -> TooltipView {
        switch element {
        case .planet(let planet):
            return TooltipView(
                title: planet.localizedName,
                symbol: planet.symbol,
                quickMeaning: planet.simpleMeaning,
                detailsButton: true
            )
        // ...
        }
    }
}
```

#### 4.2 Глоссарий терминов
- Доступ через кнопку [?] в хедере
- Поиск по терминам
- Категоризация: Планеты, Знаки, Дома, Аспекты
- Примеры из карты пользователя

### 5. Улучшенная визуализация

#### 5.1 Цветовое кодирование
```swift
struct ChartColorScheme {
    // Элементы по стихиям
    let fire = Color(hex: "#FF6B6B")      // Овен, Лев, Стрелец
    let earth = Color(hex: "#4ECDC4")     // Телец, Дева, Козерог
    let air = Color(hex: "#FFE66D")       // Близнецы, Весы, Водолей
    let water = Color(hex: "#95B8F6")     // Рак, Скорпион, Рыбы
    
    // Аспекты по типу
    let harmonious = Color.green.opacity(0.6)  // Трины, секстили
    let challenging = Color.red.opacity(0.6)   // Квадраты, оппозиции
    let neutral = Color.gray.opacity(0.4)      // Остальные
}
```

#### 5.2 Интерактивные элементы
- **Hover/Tap эффекты**: Увеличение и свечение
- **Связанные элементы**: При выборе планеты подсвечиваются её аспекты
- **Фильтрация**: Возможность скрыть/показать группы элементов
- **Зум**: Пинч для увеличения секций карты

### 6. Персонализированные интерпретации

#### 6.1 Структура интерпретаций
```swift
struct Interpretation {
    let title: String           // "Солнце в Льве"
    let emoji: String          // "☀️♌"
    let oneLiner: String       // "Вы - прирожденный лидер"
    let summary: String        // 2-3 предложения
    let detailed: String       // Полный текст
    let keywords: [String]     // ["лидерство", "творчество", "щедрость"]
    let lifeAreas: [String]    // ["карьера", "творчество", "дети"]
}
```

#### 6.2 Уровни детализации
- **Эмодзи-режим**: Только иконки и ключевые слова
- **Краткий режим**: Заголовки и одно предложение
- **Полный режим**: Развернутые интерпретации

### 7. Карточки планет (улучшенные)

#### 7.1 Новый дизайн карточки
```swift
struct PlanetCard: View {
    var body: some View {
        VStack(spacing: 12) {
            // Заголовок
            HStack {
                PlanetIcon(planet: planet)
                Text(planet.name)
                Spacer()
                InfoButton { showDetails() }
            }
            
            // Визуальное представление
            ZodiacSignBadge(sign: planet.sign)
            HouseBadge(house: planet.house)
            
            // Ключевая интерпретация
            Text(planet.coreInterpretation)
                .font(.footnote)
            
            // Индикаторы особенностей
            HStack {
                if planet.isRetrograde { RetrogradeIndicator() }
                if planet.isDomicile { DomicileIndicator() }
                if planet.isExalted { ExaltedIndicator() }
            }
        }
    }
}
```

### 8. Образовательный контент

#### 8.1 Мини-уроки
```swift
struct LearningModule {
    let lessons: [Lesson] = [
        Lesson(
            id: "planets_basics",
            title: "Что такое планеты в астрологии",
            duration: "3 мин",
            interactive: true
        ),
        Lesson(
            id: "houses_meaning",
            title: "12 домов - сферы жизни",
            duration: "5 мин",
            hasQuiz: true
        )
    ]
}
```

#### 8.2 Интерактивные примеры
- Анимации движения планет
- Сравнение "до/после" для понимания аспектов
- Примеры из жизни знаменитостей

### 9. Система прогресса изучения

#### 9.1 Отслеживание прогресса
```swift
struct ChartExplorationProgress {
    var exploredPlanets: Set<PlanetType> = []
    var readInterpretations: Set<String> = []
    var completedLessons: Set<String> = []
    
    var completionPercentage: Int {
        // Расчет процента изученности карты
    }
    
    var nextSuggestion: String {
        // Что изучить следующим
    }
}
```

#### 9.2 Достижения и мотивация
- Бейджи за изучение элементов
- Streak за ежедневное изучение
- Разблокировка продвинутых функций

### 10. Социальные функции

#### 10.1 Шаринг отдельных аспектов
```swift
struct ShareableChartElement {
    func generateShareCard() -> UIImage {
        // Красивая карточка с выбранным элементом
        // Брендинг приложения
        // QR-код для скачивания
    }
}
```

#### 10.2 Сравнение с друзьями
- Кнопка "Сравнить" на элементах
- Показ различий и сходств
- Интерпретации совместимости

---

## 🎨 UI/UX требования

### Визуальная иерархия
1. **Первичная информация**: Крупно, контрастно, с иконками
2. **Вторичная информация**: Средний размер, приглушенные цвета
3. **Детали**: Мелкий шрифт, по запросу

### Принципы дизайна
- **Progressive Disclosure**: Показывать информацию постепенно
- **Recognition over Recall**: Визуальные подсказки вместо запоминания
- **Consistency**: Единообразие элементов и взаимодействий
- **Feedback**: Четкая обратная связь на все действия

### Адаптивность
- Оптимизация для разных размеров экранов iPhone
- Поддержка ландшафтной ориентации для карты
- Dynamic Type для accessibility

### Цветовая схема (обновленная)
```swift
extension Color {
    // Основные цвета остаются космическими
    static let cosmicBackground = LinearGradient(/*...*/)
    
    // Добавляем дружелюбные акценты
    static let beginnerGreen = Color(hex: "#10B981")
    static let infoBlue = Color(hex: "#3B82F6")
    static let warningAmber = Color(hex: "#F59E0B")
    
    // Семантические цвета
    static let positive = Color(hex: "#34D399")
    static let challenging = Color(hex: "#F87171")
    static let neutral = Color(hex: "#9CA3AF")
}
```

### Анимации и переходы
- Плавные переходы между вкладками (0.3s)
- Bounce-эффект при появлении карточек
- Parallax-эффект при скролле
- Морфинг иконок при изменении состояний

---

## 📊 Метрики успеха

### Количественные метрики
- **Время до первого понимания**: < 30 секунд для новичка
- **Completion Rate онбординга**: > 80%
- **Возвращаемость на экран**: увеличение на 40%
- **Время на экране**: увеличение на 25%
- **Шаринг элементов**: > 30% пользователей

### Качественные метрики
- Снижение вопросов в поддержку о понимании карты
- Позитивные отзывы о понятности
- Рост вовлеченности в образовательный контент

---

## 🔧 Техническая реализация

### Этапы внедрения

#### Фаза 1 (1-2 недели) - Базовые улучшения
- [ ] Режимы отображения (Новичок/Расширенный/Эксперт)
- [ ] Базовые подсказки на элементах
- [ ] Упрощенная структура с вкладками
- [ ] Улучшенные карточки планет

#### Фаза 2 (1 неделя) - Онбординг и обучение
- [ ] Интерактивный тур для новичков
- [ ] Глоссарий терминов
- [ ] Контекстные подсказки
- [ ] Первые 3 мини-урока

#### Фаза 3 (1 неделя) - Персонализация
- [ ] Многоуровневые интерпретации
- [ ] Система прогресса изучения
- [ ] Достижения
- [ ] Рекомендации "что изучить дальше"

#### Фаза 4 (3-4 дня) - Социальные функции
- [ ] Генерация карточек для шаринга
- [ ] Сравнение элементов с друзьями
- [ ] Сохранение избранных интерпретаций

### Ключевые компоненты для разработки

#### 1. `ChartDisplayModeManager`
```swift
class ChartDisplayModeManager: ObservableObject {
    @Published var currentMode: DisplayMode = .beginner
    @Published var hasCompletedOnboarding: Bool = false
    @Published var userSkillLevel: SkillLevel = .novice
    
    func filterContent(for mode: DisplayMode, chart: BirthChart) -> FilteredChart
    func shouldShowElement(_ element: ChartElement) -> Bool
    func getInterpretationLevel() -> InterpretationDepth
}
```

#### 2. `TooltipService`
```swift
protocol TooltipServiceProtocol {
    func getTooltip(for element: ChartElement) -> TooltipData
    func getGlossaryDefinition(for term: String) -> GlossaryEntry?
    func getContextualHelp(for screen: ScreenContext) -> [HelpItem]
}
```

#### 3. `ChartEducationService`
```swift
class ChartEducationService {
    func getNextLesson() -> Lesson?
    func trackProgress(element: ChartElement, action: UserAction)
    func generateAchievements() -> [Achievement]
    func getSuggestions(based on: UserProgress) -> [Suggestion]
}
```

#### 4. `InterpretationEngine`
```swift
class InterpretationEngine {
    func generateInterpretation(
        for element: ChartElement,
        depth: InterpretationDepth,
        userLevel: SkillLevel
    ) -> Interpretation
    
    func personalizeText(
        template: String,
        context: UserContext
    ) -> String
}
```

### Обновление существующих компонентов

#### `ChartView` - главные изменения:
```swift
struct ChartView: View {
    @StateObject private var viewModel = ChartViewModel()
    @StateObject private var modeManager = ChartDisplayModeManager()
    @StateObject private var educationService = ChartEducationService()
    @State private var selectedTab: ChartTab = .overview
    @State private var showOnboarding: Bool = false
    
    var body: some View {
        ZStack {
            // Основной контент
            VStack(spacing: 0) {
                ChartHeaderView(modeManager: modeManager)
                ChartVisualizationView(
                    chart: viewModel.filteredChart,
                    mode: modeManager.currentMode
                )
                ChartTabBar(selectedTab: $selectedTab)
                ChartContentView(
                    tab: selectedTab,
                    chart: viewModel.birthChart,
                    mode: modeManager.currentMode
                )
            }
            
            // Слой онбординга
            if showOnboarding {
                ChartOnboardingOverlay(
                    onComplete: { showOnboarding = false }
                )
            }
        }
    }
}
```

### Интеграции

#### Analytics события
```swift
enum ChartAnalyticsEvent {
    case modeChanged(from: DisplayMode, to: DisplayMode)
    case onboardingCompleted(duration: TimeInterval)
    case elementExplored(element: ChartElement)
    case interpretationRead(id: String, depth: InterpretationDepth)
    case lessonCompleted(lessonId: String)
    case elementShared(element: ChartElement)
}
```

#### Персистентность прогресса
```swift
extension UserDefaults {
    var chartExplorationProgress: ChartExplorationProgress
    var preferredDisplayMode: DisplayMode
    var completedLessons: Set<String>
    var viewedTooltips: Set<String>
}
```

---

## 🚀 Критерии приемки

### Функциональные критерии
- [ ] Три режима отображения работают корректно
- [ ] Онбординг запускается при первом открытии
- [ ] Все подсказки отображаются корректно
- [ ] Вкладки переключаются плавно
- [ ] Интерпретации соответствуют уровню пользователя
- [ ] Прогресс изучения сохраняется
- [ ] Шаринг генерирует корректные изображения

### Нефункциональные критерии
- [ ] Производительность: отрисовка < 16ms (60 FPS)
- [ ] Время загрузки экрана < 1 секунды
- [ ] Отсутствие memory leaks
- [ ] Поддержка VoiceOver
- [ ] Работа в offline-режиме (кроме шаринга)

### UX критерии
- [ ] Новичок понимает основы карты за 2 минуты
- [ ] Опытный пользователь может отключить помощь
- [ ] Навигация интуитивна без инструкций
- [ ] Визуальная иерархия четкая
- [ ] Обратная связь на действия мгновенная

---

## 📝 Дополнительные замечания

### Приоритеты
1. **Критично**: Режимы отображения и базовые подсказки
2. **Важно**: Онбординг и структура с вкладками
3. **Желательно**: Образовательный контент и достижения
4. **Nice to have**: Продвинутая персонализация

### Риски и митигация
- **Риск**: Упрощение может оттолкнуть продвинутых пользователей
  - **Митигация**: Сохранить режим "Эксперт" с полным функционалом
  
- **Риск**: Онбординг может быть навязчивым
  - **Митигация**: Возможность пропустить, не показывать повторно

- **Риск**: Производительность при анимациях
  - **Митигация**: Использовать Metal для рендеринга карты

### Зависимости
- SwiftUI iOS 16.0+ для новых API
- Обновление `SwissEphemerisService` для фильтрации данных
- Новые endpoints Firebase для сохранения прогресса
- Дизайн-система с обновленными компонентами

---

## 📅 Оценка времени

**Общая оценка**: 4-5 недель для полной реализации

- Фаза 1: 1-2 недели
- Фаза 2: 1 неделя  
- Фаза 3: 1 неделя
- Фаза 4: 3-4 дня
- Тестирование и полировка: 3-5 дней

**Команда**: 1 iOS разработчик + поддержка дизайнера для новых компонентов

---

*Документ подготовлен для передачи в Claude Code для поэтапной реализации улучшений экрана натальной карты в приложении AstroWise.*